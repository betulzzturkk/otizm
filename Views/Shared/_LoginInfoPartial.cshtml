@if (!User?.Identity?.IsAuthenticated == true)
{
    <div class="text-center text-muted mt-3 animate__animated animate__fadeIn">
        <small>
            <i class="fas fa-info-circle me-1"></i>
            İlerlemenizi kaydetmek için giriş yapın veya kayıt olun
        </small>
    </div>
} 